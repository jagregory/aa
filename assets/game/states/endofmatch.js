// final cut scene
// send the score to the server
// and back to pre-match

function EndOfMatch(game) {
  
  this.enter = function() {
  };
  
  this.exit = function() {
  };
  
  this.tick = function() {
  };

}

module.exports = EndOfMatch;
